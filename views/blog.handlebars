<div class="container text-center">
    <h1>{{post.title}}</h1>
    <p>{{post.body}}</p>
    <p>Created by {{post.user.username}} on {{format_date post.date_created}}</p>
    <textarea></textarea>
</div>
